[package]
name = "siyuanmaster"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "SiYuanMaster CLI: capability catalog dump, scoped-token issue/verify, SafeWriteTxn transaction lifecycle over a store file, and old-config migration check."

[dependencies]
siyuanmaster-core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
