[package]
name = "siyuanmasterd"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "SiYuanMaster local gateway: health check, scoped-token verification, capability catalog, audit (metadata only), and the SafeWriteTxn write-transaction lifecycle. The administrator-level SiYuan API token never leaves the trusted boundary."

[dependencies]
siyuanmaster-core = { path = "../core" }
serde = { workspace = true }
serde_json = { workspace = true }
