{
  "name": "siyuanmaster",
  "version": "0.4.0",
  "private": true,
  "description": "SiYuanMaster — policy-aware SiYuan access boundary and safe write tools for trusted local agents.",
  "scripts": {
    "generate": "node scripts/generate-capabilities.mjs",
    "check:catalog": "node scripts/check-capabilities.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "pnpm run generate && pnpm run check:catalog && pnpm run typecheck && pnpm run test && node scripts/build.mjs"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "esbuild": "^0.25.6",
    "siyuan": "1.2.3",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=24"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
